services:

  mongodb:
    image: mongo:7
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"

  backend:
    build: .
    container_name: power-store-backend
    ports:
      - "5000:5000"
    depends_on:
      - mongodb
